# Historical filename redirect · Ancient China Pack V0.1

Status: **compatibility / historical path**  
Last updated: 2026-09-04

This filename is retained so old links do not break.

The product taxonomy has been corrected:

```text
东方古代（Realm）
└── 架空王朝（first open World Pack）
```

The current normative pack specification is:

- [`docs/FICTIONAL_DYNASTY_PACK_V0.md`](FICTIONAL_DYNASTY_PACK_V0.md)

The repository package path and stable machine identifiers still use the historical engineering name `ancient-china` during V0.1. They are compatibility identifiers, not the current human-facing product label.

Do not rename those stable IDs without an explicit manifest / permission-profile / forum-data migration and deterministic compatibility tests.

The previous full contents of this document remain available in Git history for archaeology, but should no longer be treated as current product naming truth.
